<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.StudentData" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <!-- Customize your light theme here. -->
<!--         <item name="colorPrimary">@color/lightBlue</item>-->
    </style>

    <style name="TextInputStyle">
        <item name="helperText">@string/error_txt</item>
        <item name="android:layout_width">340dp</item>
        <item name="helperTextTextColor">@color/red</item>
        <item name="hintEnabled">false</item>
        <item name="backgroundTint">@color/lightBlue</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginTop">30dp</item>
    </style>

    <style name="EditTextStyle">
<!--        <item name="android:background">@drawable/et_bg</item>-->
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:padding">10dp</item>
        <item name="android:drawableTint">@color/lightBlue</item>
        <item name="backgroundTint">@color/lightBlue</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:textColorHint">@color/black</item>
        <item name="android:textSize">18sp</item>
        <item name="android:background">@drawable/edit_text</item>
    </style>

    <style name="preFixStyle">
        <item name="android:textSize">18sp</item>
    </style>

    <style name="Theme.StudentData" parent="Base.Theme.StudentData" />
</resources>